const express = require('express')
const router = express.Router()
const connectToDatabase = require('../middleware/connectToDB').connectToDatabase

// put custom view related routes here

module.exports = router;